type Query {
 allBooks: [Book]
 book(id: String): Book
}

type Mutation {
 newBook(isn: String, title: String, publisher: String) : Book!
}

type Book {
  isn: String
  title: String
  publisher: String
  authors: [String]
  publishedDate: String
}